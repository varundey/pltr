{
  "name": "pltr",
  "version": "1.0.0",
  "description": "A browser extension which shows you the page progress as you advance through the page",
  "scripts": {
    "clean-build": "rm -r dist/*",
    "dev": "npm run clean-build && webpack --mode development",
    "build": "npm run clean-build && webpack --mode production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Varun Dey",
  "license": "MIT",
  "devDependencies": {
    "copy-webpack-plugin": "^5.0.4",
    "css-loader": "^3.2.0",
    "style-loader": "^1.0.0",
    "webpack": "^4.39.1",
    "webpack-cli": "^3.3.12"
  }
}
